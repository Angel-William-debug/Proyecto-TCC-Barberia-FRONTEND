/** Tailwind v4 se integra por PostCSS. No hay archivo tailwind.config.js:
 *  la configuracion vive en CSS, en packages/ui/src/estilos.css. */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
